# 一盏 · 网页泡茶交互原型 — 设计方案 v1

## 0. 定位

不是游戏，是一件"可以摩挲的数字物件"（参考榫卯 / Strange Horticulture / Potion Craft / Unpacking 的音效哲学）。
单一画面、无导航、无失败、无目标。用户在一张桌面上拈草药、注水、等待，最后得到一杯被"命名"出来的茶和一张手写茶签。

核心验证目标（本原型只为验证这三件事，其他一切从简）：
1. **拈起草药 → 落入水中**的手感（重量感、滞后、涟漪、声音）
2. **注水**的手感（壶体倾斜、水流粗细随倾角变化、水位上升）
3. **等待**的质感（茶色晕开、草药舒展浮沉、热气、茶签浮现）

## 1. 技术形态

- 纯前端单页：`index.html` + `style.css` + `main.js`（可拆模块，用 ES modules，无构建步骤，双击 index.html 或起静态服务即可跑）
- **不引入任何外部依赖/框架/素材文件**。所有视觉用 CSS + Canvas 2D 绘制（草药、杯、壶都用代码画——有机的贝塞尔形状即可，不追求写实，追求"手绘工笔"的气质）；所有声音用 WebAudio 合成（详见 §6）
- 逻辑与渲染分离：`brew.js` 是纯函数模块——输入（草药组合, 注水量, 浸泡时间）→ 输出（茶汤颜色, 茶名, 功效文案, 风味描述）。渲染层只演出这个状态
- 目标平台：桌面浏览器优先（鼠标），但指针事件用 Pointer Events 写，天然兼容触屏
- 画布尺寸：自适应视口，内部逻辑坐标固定 1200×800，等比缩放居中

## 2. 场景（固定机位的 2.5D 静物画）

一个画面，微俯视角（约 35°，通过椭圆透视暗示，不用真 3D）：

- **背景**：暖米色宣纸质感（CSS 渐变 + 细噪点），右上角一片斜向的柔和窗光（大的低透明度渐变），整体色调：米白、暖木色、黛青点缀
- **桌面**：深暖色木纹（Canvas 程序化画几条含蓄的木纹曲线即可），占画面下 2/3
- **左侧**：一只浅陶盘，摆 5 味草药（见 §3），每味下方极小的手写体名字（如"菊花"），常态半透明，悬停时清晰
- **中央偏右**：一只**玻璃杯**（选玻璃而非瓷，因为要看见茶色晕开和草药浮沉——这是核心表演区）。杯用椭圆杯口 + 透明杯壁高光表现
- **右侧**：一把侧提陶壶，壶嘴朝杯
- **文字出现的地方**：茶签从杯右侧缓缓浮出（见 §5）；草药介绍浮现在被拈起的草药旁（见 §3）

光标进入画面后隐藏系统光标，画一个柔和的小圆点/指尖影子，增强"手"的在场感。

## 3. 五味草药（数据内置在 brew.js）

| id | 名 | 形态要点（Canvas 绘制） | 性味一句话 | 汤色贡献 (HSL 倾向) |
|----|----|----|----|----|
| chrysanthemum | 菊花 | 米白花瓣簇，落水后花瓣缓缓张开 | 甘、微寒；清肝明目 | 浅金黄 |
| goji | 枸杞 | 橘红小椭圆粒×若干 | 甘、平；滋补肝肾 | 暖橙红 |
| mint | 薄荷 | 两三片锯齿绿叶 | 辛、凉；疏风清咽 | 极浅青绿 |
| rose | 玫瑰 | 紫红色小花苞，落水后缓慢舒展 | 甘微苦、温；疏肝解郁 | 浅粉紫 |
| licorice | 甘草 | 黄褐色斜切小圆片 | 甘、平；调和诸药 | 蜜黄 |

**交互**：
- 悬停：草药轻微上浮 1-2px + 名字清晰化
- **长按（>400ms）不拖动**：草药被拈至眼前（放大 1.6 倍升起），旁边竖排浮出两行小字介绍性味（信息藏在物件里，不做图鉴页面）。松手放回
- **按住拖动**：进入"拈"状态（见 §4.1）
- 每味可重复投放（盘中不减少），同款最多投 3 份，第 4 次拖起时那份轻轻飘回盘中（无错误提示，只是温柔地不允许）

## 4. 三个动作

### 4.1 拈（drag & drop）

- 按住草药：它被提起——放大 ~1.15×，下方投一片模糊软阴影（阴影偏移量暗示高度）
- **跟手滞后**：草药位置用弹簧/缓动追光标（如 `pos += (target - pos) * 0.12`），并根据移动速度轻微旋转倾斜——这个滞后就是"重量"
- 在杯口上方松手：草药落下（短暂重力加速），**落水瞬间**：
  - 一声轻"扑通"（WebAudio 合成，见 §6）
  - 杯内水面荡开 2-3 圈涟漪（水面椭圆上扩散的圆环）
  - 溅起 2-3 粒微小水珠粒子
  - 草药入水后减速下沉，随水流微微摆动，沉底或半浮（菊花、玫瑰浮，枸杞、甘草沉，薄荷半浮）
- 不在杯口松手：草药以一条柔和的弧线飘回陶盘原位（不是瞬移）
- **投茶可以在注水前或注水后**——干投则草药静置杯底，注水时被水流冲得翻滚浮起（这个瞬间很值得做）

### 4.2 注（pour）

- 按住壶拖向杯口：壶跟随光标，**倾角由壶嘴到杯口的相对位置连续决定**（越过杯口越低，倾角越大）
- 倾角超过阈值出水：水流是一条从壶嘴到水面的贝塞尔弧线，**粗细随倾角变化**，末端接触水面处有细碎白色扰动
- 水位（杯内椭圆水面）平滑上升；注水时已有草药被扰动翻滚
- 声音：注水白噪声，**音调随水位升高而变高、将满时变闷**（真实物理：容器共振频率随空腔缩小上升）
- 水位到杯高 85% 为满，之后水流自动减弱收束（壶"体贴地"收手，不做溢出惩罚）
- 松手：壶缓动回原位
- 水的温度不做变量（本版恒为热水），但注水后杯口立即开始冒热气

### 4.3 候（steep — 无操作的表演）

注水完成且杯中有草药后自动开始，**约 20-25 秒**，期间没有任何 UI、没有进度条、不可跳过：

- **茶色晕开**：从每片草药周围一丝丝洇出颜色，逐渐扩散混合至整杯。实现：离屏 canvas 低分辨率（如 96×64）做简易扩散模拟（每帧对浓度场做模糊扩散 + 从草药位置注入浓度），叠加一层慢速滚动的 Perlin/值噪声让边缘不规则，再放大绘制到杯内区域（放大自带的柔和恰好是水感）
- 草药继续舒展：菊花/玫瑰花瓣用几段贝塞尔控制点缓慢外扩；枸杞泡发微微变大变润
- **热气**：2-3 缕半透明白色曲线粒子，从杯口缓慢上升、扭动、消散（正弦扰动 + 透明度衰减）
- 环境音继续（见 §6），此外无事发生——这段"被允许的无所事事"就是产品本体

## 5. 茶签（结果呈现）

浸泡结束，一张竖排茶签（米色纸质感、细边框、轻微旋转 1-2°）从杯右缓缓浮出（1.5s 淡入 + 上移）：

- **茶名**：由配方生成，规则内置于 brew.js。例：
  - 单味 → 「菊花清饮」
  - 菊花+枸杞 → 「杞菊明目茶」；玫瑰+薄荷 → 「疏风解语饮」；含甘草 → 名末缀「和」字，等
  - 未收录的组合用模板生成：「〔主料名〕〔配料意象〕饮」，并写一句温柔幽默的风味注（无失败设计：任何组合都成立）
- **功效**：两三行小字，由各草药功效句拼合润色（brew.js 里为每种组合规模写好拼接模板）
- **汤色名**：给最终颜色起个雅名（浅金→"秋香"，粉紫→"藕荷"，青绿→"天水碧"……按 HSL 区间映射）
- 字体：系统楷体优先 `font-family: "Kaiti SC", "STKaiti", "KaiTi", serif`
- 茶签浮现同时一声很轻的纸声/风铃点缀音
- 茶签出现后，杯可以继续被欣赏；左下角浮现一个极淡的小字「再沏一盏」，点击后一切柔和淡出、水倒空、回到初始（重置也要有仪式感，1.5s 的淡出而非闪切）

## 6. 声音（WebAudio 全合成，无音频文件）

质感的一半是声音。首次 pointerdown 时初始化 AudioContext（浏览器自动播放策略）。

| 事件 | 合成方法 |
|----|----|
| 环境底噪 | 极轻的粉红噪声 + 缓慢音量呼吸（约 -36dB，似有若无的室内感） |
| 拈起草药 | 极短的高频轻擦（band-passed noise burst, 5ms attack） |
| 干茶落盘/落杯底 | 短促低通噪声 tick，每味草药中心频率略不同（枸杞更实、菊花更沙） |
| 落水"扑通" | 正弦短音从 ~400Hz 快速滑落至 ~150Hz（20ms）+ 一点噪声尾 |
| 注水 | 白噪声过 band-pass，中心频率 = f(水位)：从 ~400Hz 随水位升至 ~900Hz，将满时 Q 值升高变"闷"；音量随水流粗细 |
| 涟漪/搅动 | 低频正弦轻微颤动一下 |
| 茶签浮现 | 单音三角波，像很轻的风铃（一声即可，不做旋律） |

所有音量都克制，宁轻勿吵。提供左下角一个极小的静音图标。

## 7. 视觉基调速查

- 配色：`#f5f0e6`（宣纸底）`#8a6f4d`（木）`#3d4a3e`（黛青文字）`#b03a2e` 仅用于枸杞等物件本身，UI 不用鲜色
- 动画哲学：**没有任何东西瞬移或瞬变**。一切位移用缓动（ease-out ≥ 300ms），一切出现用淡入。拖拽跟手用弹簧
- 文字极少，出现时必须是"物件的一部分"（茶签、草药名牌），永远不出现按钮式 UI（除静音和"再沏一盏"两个淡化入口）

## 8. 文件结构

```
tea/
  index.html
  style.css
  js/
    main.js        // 启动、resize、指针事件路由、主循环 (rAF)
    scene.js       // 静物绘制：桌、盘、杯、壶、光
    herbs.js       // 草药数据引用 + 绘制 + 拈起/下落/舒展状态机
    water.js       // 水位、水面、涟漪、注水流、扩散场（茶色）
    steam.js       // 热气粒子
    brew.js        // 纯函数：配方 → {汤色, 茶名, 功效文案, 汤色名}（含全部文案数据）
    audio.js       // WebAudio 合成器集合
    label.js       // 茶签渲染与浮现动画
```

## 9. 验收清单（子代理自测）

- [ ] 双击 index.html（或 `python3 -m http.server`）即可运行，控制台无报错
- [ ] 拖菊花入杯：有滞后跟手感、软阴影、落水声、涟漪、花瓣张开
- [ ] 未注水先投茶、先注水后投茶两条路径都自然
- [ ] 注水：壶倾斜、水流弧线粗细变化、水位上升、音调变化、85% 自动收
- [ ] 等待 20s+：茶色从草药处晕开且混色正确（菊花+枸杞 ≈ 暖金橙）、热气可见
- [ ] 茶签浮现：名字/功效/汤色名与配方对应；至少内置 6 个具名组合 + 通用模板兜底
- [ ] 长按草药可看性味介绍
- [ ] 「再沏一盏」可柔和重置
- [ ] 全程无按钮式 UI、无进度条、无失败状态
